# IMPORTANT: We use Rancher Desktop for containers, NOT Docker
# Frontend runs on port 3003 (updated from 3002)
# Backend API runs on port 8000

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/prsnl
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=

# Service Ports - Exclusive Port Ownership
# Each service owns its port exclusively to prevent conflicts
FRONTEND_PORT=3003      # SvelteKit frontend (dev mode)
BACKEND_PORT=8000       # FastAPI backend
API_PORT=8000           # Alias for BACKEND_PORT
POSTGRES_PORT=5432      # PostgreSQL database
REDIS_PORT=6379         # Redis cache
PROMETHEUS_PORT=9090    # Prometheus metrics (if enabled)
GRAFANA_PORT=3000       # Grafana dashboard (if enabled)
