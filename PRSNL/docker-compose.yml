version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: prsnl_db
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  postgres_data:
